# Data

`extraction/` contains gold condition-scoped tuples, `probes/` contains the full
handcrafted probe benchmark with gold labels and grouping IDs, and `splits/` contains
the exact released partitions and assignment metadata. See `../docs/data_dictionary.md`
and `../docs/split_protocol.md` before transforming these files.

