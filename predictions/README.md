# Predictions

Each file contains identifiers, gold labels, and saved class decisions sufficient to
recompute the reported metrics. Raw generations, explanations, probabilities, and
model weights are intentionally excluded from this compact GitHub release. The exact
run-to-file mapping is `../manifests/run_manifest.csv`.

